'use client';

import { MessagesView } from '@/components/MessagesView';

/** Talent messages (P5) — real threads via the shared two-pane view. */
export default function TalentMessagesPage() {
  return <MessagesView role="talent" />;
}
